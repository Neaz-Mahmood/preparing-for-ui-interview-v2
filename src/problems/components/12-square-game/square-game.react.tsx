import css from './square-game.module.css'
import flex from '@course/styles'
import cx from '@course/cx'

type TProps = {
  initState?: Array<Array<number | null>>
}

export const SquareGame = (props: TProps) => {
  return <div>TODO: Implement</div>
}
